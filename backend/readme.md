convert it to standard oracle fusion fbdi file.
integration will read the file transform into fbdi and then deploy to OIC instance whichever we want it to deploy.

Source selectables, target select instance to be deployed to.


1. Generate IAR based on the instructions.
   Instructions - read from source, convert to FBDI, upload to oracle fusion instance - selected.
2. Once the IAR is generated, deploy the IAR to target instance - to be selected by User among available instances.


CREATE INTEGRATION CODE AND JUST PASS IT TO POST CALL 



Create a chat based application - LLM will have prompt - prompt should be to get instructions from user input to create
integration code - in standard iar format - considering all standard rules of developing it.

Instructions - user has to give.



iar file - deploy to oic instance which does it.

do you want me to deploy iar file to - dropdown of oic instances

Create and Deploy OIC Integration.